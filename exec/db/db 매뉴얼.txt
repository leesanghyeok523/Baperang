ddl.sql을 사용하여 mysql에 테이블을 먼저 만든다.

그리고 school_data_updated.sql을 넣는다.(spring 실행시 자동으로 실행하여 넣어진다)

menu_inserts.sql 사용한다.

menu_nutrient_inserts_corrected.sql 사용한다.

students_inserts.sql 사용한다.

leftover.sql 사용한다.

leftover_03_04_month 사용한다

leftover_update 사용한다.

holiday.sql 사용한다

inventory_inserts.sql을 사용한다.


이 순서대로 하면 db에 기본적인 데이터가 넣어진다.